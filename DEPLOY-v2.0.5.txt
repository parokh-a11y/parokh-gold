Deploy v2.0.5
1) Copy files to D:\GitHub\parokh-gold
2) Secrets (once):
   npx wrangler secret put SUPABASE_URL
   npx wrangler secret put SUPABASE_SERVICE_KEY
   npx wrangler secret put LICENSE_BOT_TOKEN
   npx wrangler secret put LICENSE_ADMIN_CHAT_IDS
3) npx wrangler deploy

Test: buy on site → TXID + broker + account → license bot gets Activate/Reject
