name = "parokh-gold"
compatibility_date = "2026-07-31"
main = "worker.js"

[assets]
directory = "."
binding = "ASSETS"
# IMPORTANT: do not treat /api as SPA
html_handling = "auto"
not_found_handling = "single-page-application"

# Exclude api from SPA fallback if supported by runtime
