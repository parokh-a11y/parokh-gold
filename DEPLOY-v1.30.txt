PAROKH GOLD v1.30 — Payment System V1

Deploy:
  npx wrangler deploy

Secrets:
  npx wrangler secret put PAYMENT_WALLET_ADDRESS
  npx wrangler secret put TRON_RPC_URL
  # optional:
  npx wrangler secret put USDT_TRON_CONTRACT
  npx wrangler secret put TRONGRID_API_KEY
  npx wrangler secret put TRON_RPC_AUTH

Default TRON RPC: https://api.trongrid.io
Default USDT TRC-20: TR7NHqjeKQxGTCi8q8ZY4pL8otSzgjLj6t

API:
  POST /api/payment/create
  GET|POST /api/payment/info
  POST /api/payment/submit-txid
  POST /api/payment/verify

Flow: login → select plan → create order → payment panel → pay USDT TRC20 → submit TXID → auto verify
